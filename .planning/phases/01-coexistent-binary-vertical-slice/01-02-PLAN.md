---
phase: 01-coexistent-binary-vertical-slice
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/protocol/schema/phase1_info.pb.h
  - src/protocol/schema/phase1_info.pb.c
  - src/protocol/binaryFrame.h
  - src/protocol/binaryEndpoint.h
  - src/protocol/binaryRouter.h
  - src/main.cpp
autonomous: true
requirements:
  - TRAN-01
  - TRAN-02
  - TRAN-03
  - ENVP-01
  - ENVP-02
  - ENVP-03
  - ENVP-04
  - RUNT-02
  - COMP-01
  - SLCE-01
must_haves:
  truths:
    - "A client can send one framed binary request and receive one framed binary response."
    - "Malformed, corrupted, and oversize frames are rejected safely before business logic runs."
    - "JSON requests starting with `{` still use the legacy path unchanged."
  artifacts:
    - path: "src/protocol/binaryEndpoint.h"
      provides: "Byte-oriented serial frame reader/writer for the new protocol"
      contains: "embrace"
    - path: "src/protocol/binaryRouter.h"
      provides: "Operation 0x01 router that reuses existing info resolver logic"
      contains: "query_info"
    - path: "src/main.cpp"
      provides: "Transport discrimination between JSON and binary on the same ports"
      contains: "peek"
  key_links:
    - from: "src/main.cpp"
      to: "src/deviceEndpoint.h"
      via: "first-byte JSON routing"
      pattern: "'{'"
    - from: "src/main.cpp"
      to: "src/protocol/binaryEndpoint.h"
      via: "sync-byte binary routing"
      pattern: "0xA5"
    - from: "src/protocol/binaryRouter.h"
      to: "src/domain/info/resolvers.h"
      via: "existing info resolver adapter"
      pattern: "query_info"
---

<objective>
Implement the binary frame parser, binary endpoint, and `info.query` router while preserving the existing JSON path untouched.

Purpose: Deliver the actual Phase 1 firmware vertical slice against the contract from Plan 01.
Output: Generated schema bindings, frame/parser code, binary serial endpoint, router for operation `0x01`, and `main.cpp` coexistence wiring.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-coexistent-binary-vertical-slice/01-RESEARCH.md
@.planning/phases/01-coexistent-binary-vertical-slice/01-01-PLAN.md
@src/main.cpp
@src/deviceEndpoint.h
@src/domain/info/resolvers.h
@src/domain/station/modbus/modbusPacket.h

<interfaces>
From `src/main.cpp`:

```cpp
bool isDeviceDataComing = rpiEndpoint->embrace(&requestString);
bool isEndpointDataComing = serialEndpoint->embrace(&requestString);
```

From `src/domain/station/modbus/modbusPacket.h`:

```cpp
FastCRC16 crc16;
static bool verifyPacket(const std::vector<byte> &vPacket)
```

From `src/domain/info/resolvers.h`:

```cpp
DynamicJsonDocument resolve(JsonObject reqData, CombineContext *context) override;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement generated schema bindings and bounded frame codec</name>
  <files>src/protocol/schema/phase1_info.pb.h, src/protocol/schema/phase1_info.pb.c, src/protocol/binaryFrame.h</files>
  <behavior>
    - Test 1: a valid `info.query` request body encodes and decodes with version/op/request ID/payload intact
    - Test 2: CRC mismatch returns canonical frame failure without payload decode
    - Test 3: declared oversize body length is rejected before buffer copy beyond the configured max
    - Test 4: unsupported version and unknown operation map to canonical status codes
  </behavior>
  <action>Generate and check in the nanopb bindings from `schema/binary/phase1_info.proto` under `src/protocol/schema/`. In `src/protocol/binaryFrame.h`, implement the fixed frame encode/decode helpers using the Plan 01 constants: sync bytes `0xA5`/`0x5A`, little-endian length fields, CRC16 via the existing `FastCRC16` dependency, hard body/frame ceilings, and a fixed envelope carrying version, message type, operation ID, request ID, status, and payload length. Reject malformed frames, CRC failures, oversize lengths, unsupported versions, and payload decode failures with canonical statuses instead of falling through to domain logic.</action>
  <verify>
    <automated>rtk pio test -e native_protocol</automated>
  </verify>
  <done>The repo contains checked-in generated bindings and a frame helper that passes native tests for valid, malformed, CRC-failed, oversize, and unsupported-version cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire binary serial handling and route operation 0x01 through existing info logic</name>
  <files>src/protocol/binaryEndpoint.h, src/protocol/binaryRouter.h, src/main.cpp</files>
  <behavior>
    - Test 1: first byte `{` still routes to the legacy JSON flow unchanged
    - Test 2: sync bytes route to the binary endpoint and return a framed response on the originating port
    - Test 3: operation `0x01` returns version/model/device_env sourced from existing `query_info` logic
    - Test 4: malformed binary traffic does not crash and does not consume the JSON path incorrectly
  </behavior>
  <action>Create `src/protocol/binaryEndpoint.h` to read/write framed binary messages on the same `HardwareSerial` ports used today, but only after `src/main.cpp` peeks and identifies the binary sync prefix. Create `src/protocol/binaryRouter.h` that maps operation `0x01` to the existing `query_info` behavior by adapting the existing resolver/domain logic instead of duplicating the payload composition. Update `src/main.cpp` so each serial source peeks the first byte: `{` stays on `DeviceEndpoint`, `0xA5` enters the new binary path, and unknown leading bytes are discarded/logged safely. Keep the current JSON request validation and response shaping unchanged for JSON traffic.</action>
  <verify>
    <automated>rtk pio test -e native_protocol && rtk pio run -e v2_dev</automated>
  </verify>
  <done>A client can send one framed binary `info.query` request and receive a framed typed response, while JSON requests still compile and route through the legacy path unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host serial client → `binaryEndpoint` | Untrusted serial bytes enter parser state and bounded buffers |
| `binaryRouter` → domain resolver | Parsed envelope/payload crosses into business logic |
| `main.cpp` transport selector → JSON / binary paths | Incorrect discrimination could break coexistence or cause parser confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | D | `src/protocol/binaryFrame.h` | mitigate | Enforce hard size ceilings before payload decode or buffer copy |
| T-01-06 | T | `src/protocol/binaryFrame.h` | mitigate | Validate CRC16 across the exact frame body before routing any request |
| T-01-07 | E | `src/protocol/binaryRouter.h` | mitigate | Accept only known version/op IDs and map all others to canonical protocol errors |
| T-01-08 | T | `src/main.cpp` | mitigate | Use first-byte discrimination so binary traffic cannot silently corrupt the existing JSON path |
| T-01-09 | S | serial transport | accept | Physical/serial-auth trust remains unchanged from the existing firmware and is outside this milestone |
</threat_model>

<verification>
- `rtk pio test -e native_protocol`
- `rtk pio run -e v2_dev`
- Inspect that `src/main.cpp` still routes JSON requests only through `DeviceEndpoint`.
</verification>

<success_criteria>
- One real binary request/response operation works end to end in firmware.
- Malformed, CRC-failed, oversize, unsupported-version, and unknown-operation inputs return canonical failures safely.
- JSON coexistence remains intact at compile-time and in transport routing logic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-coexistent-binary-vertical-slice/01-02-SUMMARY.md`
</output>
