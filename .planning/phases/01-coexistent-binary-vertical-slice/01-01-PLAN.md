---
phase: 01-coexistent-binary-vertical-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/binary/phase1_info.proto
  - schema/binary/phase1_info.options
  - src/protocol/binaryProtocol.h
  - platformio.ini
  - test/end-to-end/test_binary_protocol.cpp
autonomous: true
requirements:
  - ENVP-01
  - ENVP-02
  - ENVP-03
  - ENVP-04
  - SCMA-01
  - SCMA-02
  - SCMA-03
  - SCMA-04
must_haves:
  truths:
    - "The first binary operation has one canonical envelope and schema contract."
    - "Fast automated tests exist before firmware transport wiring begins."
    - "The schema source can evolve additively without field-number reuse."
  artifacts:
    - path: "schema/binary/phase1_info.proto"
      provides: "Canonical typed request/response schema for operation 0x01"
      contains: "message InfoQueryRequest"
    - path: "src/protocol/binaryProtocol.h"
      provides: "Binary protocol constants for version, sync bytes, op IDs, status codes, and size limits"
      contains: "SG_BINARY_PROTOCOL_VERSION"
    - path: "test/end-to-end/test_binary_protocol.cpp"
      provides: "Native end-to-end regression coverage"
      contains: "CRC"
  key_links:
    - from: "schema/binary/phase1_info.proto"
      to: "src/protocol/binaryProtocol.h"
      via: "operation and status definitions"
      pattern: "InfoQuery"
    - from: "src/protocol/binaryProtocol.h"
      to: "test/end-to-end/test_binary_protocol.cpp"
      via: "native end-to-end assertions"
      pattern: "SG_BINARY_"
---

<objective>
Define the canonical Phase 1 binary contract and create a fast host-side test loop before firmware transport wiring begins.

Purpose: Lock the irreversible protocol decisions early so later tasks build against one contract instead of exploring alternatives mid-implementation.
Output: Canonical `.proto` schema, envelope constants, native test environment, and initial protocol regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-coexistent-binary-vertical-slice/01-RESEARCH.md
@src/domain/info/resolvers.h
@src/deviceEndpoint.h

<interfaces>
From `src/domain/info/resolvers.h`:

```cpp
class query_info : public Query {
public:
  explicit query_info() : Query("info") {};

  DynamicJsonDocument resolve(JsonObject reqData, CombineContext *context) override {
    DynamicJsonDocument data(128);
    data["version"] = VERSION;
    data["model"] = SG_MODEL;
    data["device_env"] = "development"; // or "prod"
    return data;
  }
};
```

From `src/deviceEndpoint.h`:

```cpp
class DeviceEndpoint {
public:
  bool embrace(String *message);
  void unleash(String message);
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the binary slice contract</name>
  <files>schema/binary/phase1_info.proto, schema/binary/phase1_info.options, src/protocol/binaryProtocol.h</files>
  <read_first>schema/binary/phase1_info.proto (if present), schema/binary/phase1_info.options (if present), src/domain/info/resolvers.h, src/domain/station/modbus/modbusPacket.h, platformio.ini</read_first>
  <action>Create the canonical Phase 1 contract for operation `0x01` (`info.query`). In `schema/binary/phase1_info.proto`, define request/response payload messages for the first operation plus any shared enums needed for payload-level typing; use proto3, apply `optional` where presence semantics matter, and reserve a range/name block so additive evolution is explicit. In `schema/binary/phase1_info.options`, cap string sizes for static allocation suitable for nanopb. In `src/protocol/binaryProtocol.h`, define the fixed transport envelope constants: sync bytes `0xA5`/`0x5A`, protocol version `1`, message type enum, operation ID `0x01`, canonical status codes, and max body/frame size constants. Do not invent a second schema source anywhere else.</action>
  <verify>
    <automated>MISSING — Wave 1 Task 2 creates `test/end-to-end/test_binary_protocol.cpp` in the dedicated `end-to-end` native suite and `native_protocol` first</automated>
  </verify>
  <acceptance_criteria>
    - `schema/binary/phase1_info.proto` contains `syntax = "proto3";`
    - `schema/binary/phase1_info.proto` contains `message InfoQueryRequest`
    - `schema/binary/phase1_info.proto` contains `message InfoQueryResponse`
    - `schema/binary/phase1_info.proto` contains a `reserved` declaration for future additive-only evolution
    - `schema/binary/phase1_info.options` contains bounded nanopb options for string fields used by the response payload
    - `src/protocol/binaryProtocol.h` contains `0xA5`, `0x5A`, protocol version `1`, operation `0x01`, and named status codes for `OK`, `BAD_FRAME`, `CRC_FAIL`, `OVERSIZE`, `UNSUPPORTED_VERSION`, `UNKNOWN_OPERATION`, `DECODE_FAIL`, and `INTERNAL_ERROR`
  </acceptance_criteria>
  <done>The repo contains one authoritative schema source and one authoritative envelope constants header, and both describe the same Phase 1 operation and status space.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add native protocol test harness and initial regression cases</name>
  <files>platformio.ini, test/end-to-end/test_binary_protocol.cpp</files>
  <read_first>platformio.ini, test/test_main.cpp, test/system/rs485.h, schema/binary/phase1_info.proto, src/protocol/binaryProtocol.h</read_first>
  <behavior>
    - Test 1: valid frame constants and declared max sizes are internally consistent
    - Test 2: CRC mismatch case is rejected by the frame helper expectations
    - Test 3: oversize body length is rejected before payload decode work
    - Test 4: schema request/response examples stay within the declared bounded sizes
  </behavior>
  <action>Add a `native_protocol` PlatformIO test environment dedicated to the `end-to-end` host-side standard C++ suite that compiles without `bootstrap()`, Arduino runtime, or attached hardware. Create `test/end-to-end/test_binary_protocol.cpp` with fast Unity tests that lock the contract from Task 1: sync bytes, envelope field order, CRC coverage range, size ceilings, and representative `info.query` request/response fixture lengths. Keep the test target host-safe by isolating protocol helpers from Arduino-only code; migrate existing utility/protocol coverage into this host-side path where feasible so contract drift is caught before firmware integration.</action>
  <verify>
    <automated>rtk pio test -e native_protocol</automated>
  </verify>
  <acceptance_criteria>
    - `platformio.ini` contains an environment named `native_protocol`
    - `test/end-to-end/test_binary_protocol.cpp` includes `unity.h`
    - `test/end-to-end/test_binary_protocol.cpp` contains tests covering CRC failure and oversize rejection
    - `rtk pio test -e native_protocol` exits 0
    - The native test environment does not require `bootstrap()` or ESP32 upload/serial access to run
  </acceptance_criteria>
  <done>`rtk pio test -e native_protocol` passes and fails if sync bytes, size bounds, CRC assumptions, or schema fixture sizes drift from the contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host serial client → binary parser | Untrusted bytes cross into the new protocol boundary |
| schema source → generated bindings | Contract drift here would break firmware/backend interoperability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `schema/binary/phase1_info.proto` | mitigate | Reserve field numbers/names and keep one canonical schema source checked into the repo |
| T-01-02 | D | `src/protocol/binaryProtocol.h` | mitigate | Declare hard max body/frame sizes and lock them with native tests before parser implementation |
| T-01-03 | R | `test/end-to-end/test_binary_protocol.cpp` | mitigate | Add explicit fixture assertions so later changes have reproducible contract evidence |
| T-01-04 | S | serial transport | accept | Phase 1 preserves the existing unauthenticated serial trust model; auth redesign is out of scope for this milestone |
</threat_model>

<verification>
- `rtk pio test -e native_protocol`
- Confirm the `.proto` file reserves retired identifiers and documents the first operation clearly.
</verification>

<success_criteria>
- One canonical schema source exists for the Phase 1 payload.
- One constants header exists for the Phase 1 envelope.
- Native tests guard CRC, oversize, and bounded-size assumptions before firmware transport code is written.
</success_criteria>

<output>
After completion, create `.planning/phases/01-coexistent-binary-vertical-slice/01-01-SUMMARY.md`
</output>
