---
phase: 01-coexistent-binary-vertical-slice
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - docs/protocol/binary-phase1-info.md
  - docs/protocol/examples/phase1-info.md
  - test/end-to-end/test_binary_coexistence.cpp
autonomous: true
requirements:
  - RUNT-01
  - RUNT-02
  - COMP-01
  - SLCE-02
must_haves:
  truths:
    - "Backend implementers have a concrete timeout/retry and status-code contract for the first binary operation."
    - "Regression tests prove malformed traffic is rejected safely and JSON coexistence remains intact."
    - "The first operation has copy-pasteable wire examples for backend encoder/decoder work."
  artifacts:
    - path: "docs/protocol/binary-phase1-info.md"
      provides: "Backend-facing binary contract for Phase 1"
      contains: "250 ms"
    - path: "docs/protocol/examples/phase1-info.md"
      provides: "Hex-level request/response examples for operation 0x01"
      contains: "0x01"
    - path: "test/end-to-end/test_binary_coexistence.cpp"
      provides: "Regression coverage for malformed frames and JSON coexistence"
      contains: "JSON"
  key_links:
    - from: "docs/protocol/binary-phase1-info.md"
      to: "docs/protocol/examples/phase1-info.md"
      via: "wire examples and status table"
      pattern: "request_id"
    - from: "test/end-to-end/test_binary_coexistence.cpp"
      to: "src/main.cpp"
      via: "transport discrimination expectations"
      pattern: "binary"
---

<objective>
Document the Phase 1 backend contract and finish the regression coverage that proves coexistence and safe failure handling.

Purpose: Turn the firmware slice into an executable handoff for the in-house backend while locking in the runtime/error behavior with tests.
Output: Backend protocol docs, hex fixtures, timeout/retry guidance, and final coexistence/malformed regression tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-coexistent-binary-vertical-slice/01-RESEARCH.md
@.planning/phases/01-coexistent-binary-vertical-slice/01-02-PLAN.md
@src/main.cpp
@src/protocol/binaryProtocol.h
@src/protocol/binaryFrame.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the backend wire contract for operation 0x01</name>
  <files>docs/protocol/binary-phase1-info.md, docs/protocol/examples/phase1-info.md</files>
  <read_first>docs/ (existing protocol examples if any), .planning/phases/01-coexistent-binary-vertical-slice/01-RESEARCH.md, src/protocol/binaryProtocol.h, src/protocol/binaryFrame.h</read_first>
  <action>Write backend-facing documentation for the Phase 1 binary slice. `docs/protocol/binary-phase1-info.md` must describe the frame layout, envelope fields, operation ID `0x01`, canonical status codes, max frame/body sizes, and the runtime contract: backend timeout `250 ms`, one retry only on timeout/transport silence, and no retry on explicit protocol failures. `docs/protocol/examples/phase1-info.md` must include at least one request and one success response in byte/hex form plus one failure example so a backend engineer can implement encoder/decoder logic without re-reading firmware code.</action>
  <verify>
    <automated>rtk pio test -e native_protocol</automated>
  </verify>
  <acceptance_criteria>
    - `docs/protocol/binary-phase1-info.md` contains the sync bytes, frame layout, operation `0x01`, request ID semantics, status code table, max sizes, and the exact `250 ms` timeout guidance
    - `docs/protocol/binary-phase1-info.md` states `1 retry only` for timeout/transport silence and `no retry` for explicit protocol failures
    - `docs/protocol/examples/phase1-info.md` contains at least one request frame, one success response frame, and one failure response frame in byte/hex form
    - `rtk pio test -e native_protocol` exits 0 after the docs/example fixtures are added
  </acceptance_criteria>
  <done>The repo contains backend-consumable docs and examples that make the Phase 1 wire contract, status handling, and retry behavior explicit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add final malformed/coexistence regression coverage</name>
  <files>test/end-to-end/test_binary_coexistence.cpp</files>
  <read_first>test/end-to-end/test_binary_protocol.cpp, src/main.cpp, src/protocol/binaryProtocol.h, src/protocol/binaryFrame.h, docs/protocol/binary-phase1-info.md, docs/protocol/examples/phase1-info.md</read_first>
  <behavior>
    - Test 1: leading `{` is classified as JSON traffic and not consumed by the binary path
    - Test 2: valid sync-prefixed binary traffic is classified as binary and reaches the frame decoder
    - Test 3: malformed CRC / oversize / unknown-op frames fail canonically without crashing
    - Test 4: backend fixture examples in docs remain consistent with the encoder/decoder helpers
  </behavior>
  <action>Create `test/end-to-end/test_binary_coexistence.cpp` in the dedicated `end-to-end` suite with regression tests focused on the failure modes most likely to break the milestone: transport discrimination between JSON and binary, malformed frame rejection, unknown operation handling, and documentation fixture drift. Keep the suite isolated from Arduino runtime and board upload requirements by exercising host-safe helpers wherever feasible. Use the actual protocol constants/helpers from the implementation rather than copied literals so docs/tests stay synchronized with firmware behavior.</action>
  <verify>
    <automated>rtk pio test -e native_protocol && rtk pio run -e v2_dev</automated>
  </verify>
  <acceptance_criteria>
    - `test/end-to-end/test_binary_coexistence.cpp` contains cases for JSON-vs-binary discrimination, malformed CRC, oversize, and unknown operation handling
    - At least one test loads or reconstructs the documented request/response examples using the real protocol constants/helpers rather than copied magic numbers
    - `rtk pio test -e native_protocol && rtk pio run -e v2_dev` exits 0
    - The regression suite remains host-side and does not require board upload to run
  </acceptance_criteria>
  <done>The final regression suite proves coexistence and malformed-input safety, and the documented wire examples stay in sync with the implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware contract docs → backend implementation | Ambiguity here can create incompatible encoders/decoders |
| test fixtures → protocol implementation | Drift here can hide broken wire behavior until integration time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | R | `docs/protocol/binary-phase1-info.md` | mitigate | Publish explicit timeout/retry/status behavior and byte-order rules so backend behavior is reproducible |
| T-01-11 | T | `docs/protocol/examples/phase1-info.md` | mitigate | Derive examples from real protocol constants/helpers and cover them with regression tests |
| T-01-12 | D | `test/end-to-end/test_binary_coexistence.cpp` | mitigate | Keep malformed-frame regression cases in the fast native `end-to-end` suite so safety checks cannot silently regress |
| T-01-13 | I | docs/examples | accept | The documented payload contains only firmware metadata, not secrets or PII |
</threat_model>

<verification>
- `rtk pio test -e native_protocol`
- `rtk pio run -e v2_dev`
- Read the docs once and confirm a backend engineer could encode `info.query` and interpret failures without opening source files.
</verification>

<success_criteria>
- The backend wire contract is explicit and copy-pasteable.
- Coexistence and malformed-input regressions are locked by automated tests.
- The first binary operation is ready for backend implementation work without additional protocol clarification.
</success_criteria>

<output>
After completion, create `.planning/phases/01-coexistent-binary-vertical-slice/01-03-SUMMARY.md`
</output>
